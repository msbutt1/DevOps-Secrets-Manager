server:
  port: 8080

database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  name: devops_secret_manager
  sslmode: disable

jwt:
  secret: change-me  # generate with: openssl rand -hex 32
  access_token_ttl: 15m
  refresh_token_ttl: 168h  # 7 days
